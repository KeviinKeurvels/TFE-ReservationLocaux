import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.getgive.ereserve',
  appName: 'E-Reserve',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
